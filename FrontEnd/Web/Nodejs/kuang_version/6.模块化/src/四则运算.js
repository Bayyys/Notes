// 工具类
const sum = (a, b) => a + b;
const minus = (a, b) => a - b;
const multiply = (a, b) => a * b;
const division = (a, b) => a / b;

// 导出
// module.exports = {
//     sum: sum,
//     minus: minus,
//     multiply: multiply,
//     division: division
// }
module.exports = {
    sum,
    minus,
    multiply,
    division
}
