const uniq = require("uniq");
const math = require("math_bayyy");

var arr = [1, 3, 2, 1, 4, 2, 3, 5, 4];

const result = uniq(arr);
console.log(math.add(1, 2));
console.log(math.minus(1, 2));

console.log(result);
